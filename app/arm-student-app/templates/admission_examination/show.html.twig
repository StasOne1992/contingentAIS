{% extends 'base.html.twig' %}

{% block title %}AdmissionExamination{% endblock %}

{% block body %}
    <h1>AdmissionExamination</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admission_examination.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ admission_examination.Date ? admission_examination.Date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admission_examination_index') }}">back to list</a>

    <a href="{{ path('app_admission_examination_edit', {'id': admission_examination.id}) }}">edit</a>

    {{ include('admission_examination/_delete_form.html.twig') }}
{% endblock %}
