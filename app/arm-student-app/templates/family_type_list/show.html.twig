{% extends 'base.html.twig' %}

{% block title %}FamilyTypeList{% endblock %}

{% block body %}
    <h1>FamilyTypeList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ family_type_list.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ family_type_list.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_family_type_list_index') }}">back to list</a>

    <a href="{{ path('app_family_type_list_edit', {'id': family_type_list.id}) }}">edit</a>

    {{ include('family_type_list/_delete_form.html.twig') }}
{% endblock %}
