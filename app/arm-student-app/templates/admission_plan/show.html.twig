{% extends 'base.html.twig' %}

{% block title %}AdmissionPlan{% endblock %}

{% block body %}
    <h1>AdmissionPlan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admission_plan.id }}</td>
            </tr>
            <tr>
                <th>TargetCount</th>
                <td>{{ admission_plan.TargetCount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admission_plan_index') }}">back to list</a>

    <a href="{{ path('app_admission_plan_edit', {'id': admission_plan.id}) }}">edit</a>

    {{ include('admission_plan/_delete_form.html.twig') }}
{% endblock %}
