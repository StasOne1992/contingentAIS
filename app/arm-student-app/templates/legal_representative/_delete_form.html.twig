<form method="post" action="{{ path('app_legal_representative_delete', {'id': legal_representative.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ legal_representative.id) }}">
    <button class="btn">Delete</button>
</form>
