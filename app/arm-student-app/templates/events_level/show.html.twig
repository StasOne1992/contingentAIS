{% extends 'base.html.twig' %}

{% block title %}EventsLevel{% endblock %}

{% block body %}
    <h1>EventsLevel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ events_level.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ events_level.Name }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ events_level.Title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_events_level_index') }}">back to list</a>

    <a href="{{ path('app_events_level_edit', {'id': events_level.id}) }}">edit</a>

    {{ include('events_level/_delete_form.html.twig') }}
{% endblock %}
