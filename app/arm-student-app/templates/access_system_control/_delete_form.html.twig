<form method="post" action="{{ path('app_access_system_control_delete', {'id': access_system_control.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ access_system_control.id) }}">
    <button class="btn">Delete</button>
</form>
