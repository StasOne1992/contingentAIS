<form method="post" action="{{ path('app_abiturient_petition_delete', {'id': abiturient_petition.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ abiturient_petition.id) }}">
    <button class="btn">Delete</button>
</form>
