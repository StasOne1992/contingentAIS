{% extends 'base.html.twig' %}

{% block title %}LoginValues{% endblock %}

{% block body %}
    <h1>LoginValues</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ login_value.id }}</td>
            </tr>
            <tr>
                <th>LoginValue</th>
                <td>{{ login_value.LoginValue }}</td>
            </tr>
            <tr>
                <th>PasswordValue</th>
                <td>{{ login_value.PasswordValue }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_login_values_index') }}">back to list</a>

    <a href="{{ path('app_login_values_edit', {'id': login_value.id}) }}">edit</a>

    {{ include('login_values/_delete_form.html.twig') }}
{% endblock %}
