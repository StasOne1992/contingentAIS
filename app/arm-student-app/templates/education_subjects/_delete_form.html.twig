<form method="post" action="{{ path('app_education_subjects_delete', {'id': education_subject.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ education_subject.id) }}">
    <button class="btn">Delete</button>
</form>
