{% extends 'base.html.twig' %}

{% block title %}EducationSubjects{% endblock %}

{% block body %}
    <h1>EducationSubjects</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ education_subject.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ education_subject.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_education_subjects_index') }}">back to list</a>

    <a href="{{ path('app_education_subjects_edit', {'id': education_subject.id}) }}">edit</a>

    {{ include('education_subjects/_delete_form.html.twig') }}
{% endblock %}
