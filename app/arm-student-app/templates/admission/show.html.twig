{% extends 'base.html.twig' %}

{% block title %}Admission{% endblock %}

{% block body %}
    <h1>Admission</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admission.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ admission.name }}</td>
            </tr>
            <tr>
                <th>DateStart</th>
                <td>{{ admission.dateStart ? admission.dateStart|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateEnd</th>
                <td>{{ admission.dateEnd ? admission.dateEnd|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admission_index') }}">back to list</a>

    <a href="{{ path('app_admission_edit', {'id': admission.id}) }}">edit</a>

    {{ include('admission/_delete_form.html.twig') }}
{% endblock %}
