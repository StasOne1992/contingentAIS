{% extends 'base.html.twig' %}

{% block title %}EventsPlaces{% endblock %}

{% block body %}
    <h1>EventsPlaces</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ events_place.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ events_place.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_events_places_index') }}">back to list</a>

    <a href="{{ path('app_events_places_edit', {'id': events_place.id}) }}">edit</a>

    {{ include('events_places/_delete_form.html.twig') }}
{% endblock %}
