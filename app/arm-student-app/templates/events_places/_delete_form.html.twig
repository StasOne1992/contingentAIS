<form method="post" action="{{ path('app_events_places_delete', {'id': events_place.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ events_place.id) }}">
    <button class="btn">Delete</button>
</form>
