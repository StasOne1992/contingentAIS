{% extends 'base.html.twig' %}

{% block title %}EventsCategory{% endblock %}

{% block body %}
    <h1>EventsCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ events_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ events_category.Name }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ events_category.Title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_events_category_index') }}">back to list</a>

    <a href="{{ path('app_events_category_edit', {'id': events_category.id}) }}">edit</a>

    {{ include('events_category/_delete_form.html.twig') }}
{% endblock %}
