{% extends 'base.html.twig' %}

{% block title %}AdmissionExaminationSubjects{% endblock %}

{% block body %}
    <h1>AdmissionExaminationSubjects</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admission_examination_subject.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ admission_examination_subject.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admission_examination_subjects_index') }}">back to list</a>

    <a href="{{ path('app_admission_examination_subjects_edit', {'id': admission_examination_subject.id}) }}">edit</a>

    {{ include('admission_examination_subjects/_delete_form.html.twig') }}
{% endblock %}
