{% extends 'base.html.twig' %}

{% block title %}AdmissionExaminationSubjects index{% endblock %}

{% block body %}
    <h1>AdmissionExaminationSubjects index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for admission_examination_subject in admission_examination_subjects %}
            <tr>
                <td>{{ admission_examination_subject.id }}</td>
                <td>{{ admission_examination_subject.Name }}</td>
                <td>
                    <a href="{{ path('app_admission_examination_subjects_show', {'id': admission_examination_subject.id}) }}">show</a>
                    <a href="{{ path('app_admission_examination_subjects_edit', {'id': admission_examination_subject.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Нет ни одной записи</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admission_examination_subjects_new') }}">Create new</a>
{% endblock %}
