{% extends 'base.html.twig' %}

{% block title %}ContingentDocumentType{% endblock %}

{% block body %}
    <h1>ContingentDocumentType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contingent_document_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ contingent_document_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contingent_document_type_index') }}">back to list</a>

    <a href="{{ path('app_contingent_document_type_edit', {'id': contingent_document_type.id}) }}">edit</a>

    {{ include('contingent_document_type/_delete_form.html.twig') }}
{% endblock %}
