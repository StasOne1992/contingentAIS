{% extends 'base.html.twig' %}

{% block title %}AbiturientPetitionStatus{% endblock %}

{% block body %}
    <h1>AbiturientPetitionStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ abiturient_petition_status.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ abiturient_petition_status.title }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ abiturient_petition_status.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_abiturient_petition_status_index') }}">back to list</a>

    <a href="{{ path('app_abiturient_petition_status_edit', {'id': abiturient_petition_status.id}) }}">edit</a>

    {{ include('abiturient_petition_status/_delete_form.html.twig') }}
{% endblock %}
