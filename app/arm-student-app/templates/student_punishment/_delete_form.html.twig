<form method="post" action="{{ path('app_student_punishment_delete', {'id': student_punishment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ student_punishment.id) }}">
    <button class="btn">Delete</button>
</form>
