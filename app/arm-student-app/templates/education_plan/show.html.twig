{% extends 'base.html.twig' %}

{% block title %}EducationPlan{% endblock %}

{% block body %}
    <h1>EducationPlan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ education_plan.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_education_plan_index') }}">back to list</a>

    <a href="{{ path('app_education_plan_edit', {'id': education_plan.id}) }}">edit</a>

    {{ include('education_plan/_delete_form.html.twig') }}
{% endblock %}
