{% extends 'base.html.twig' %}

{% block title %}AdmissionStatus{% endblock %}

{% block body %}
    <h1>AdmissionStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admission_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ admission_status.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admission_status_index') }}">back to list</a>

    <a href="{{ path('app_admission_status_edit', {'id': admission_status.id}) }}">edit</a>

    {{ include('admission_status/_delete_form.html.twig') }}
{% endblock %}
