{% extends 'base.html.twig' %}

{% block title %}LegalRepresentativeTypeList{% endblock %}

{% block body %}
    <h1>LegalRepresentativeTypeList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ legal_representative_type_list.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ legal_representative_type_list.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_legal_representative_type_list_index') }}">back to list</a>

    <a href="{{ path('app_legal_representative_type_list_edit', {'id': legal_representative_type_list.id}) }}">edit</a>

    {{ include('legal_representative_type_list/_delete_form.html.twig') }}
{% endblock %}
