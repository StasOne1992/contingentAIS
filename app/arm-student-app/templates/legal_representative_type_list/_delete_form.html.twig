<form method="post" action="{{ path('app_legal_representative_type_list_delete', {'id': legal_representative_type_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ legal_representative_type_list.id) }}">
    <button class="btn">Delete</button>
</form>
