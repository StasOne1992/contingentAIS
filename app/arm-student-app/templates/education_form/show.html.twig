{% extends 'base.html.twig' %}

{% block title %}EducationForm{% endblock %}

{% block body %}
    <h1>EducationForm</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ education_form.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ education_form.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_education_form_index') }}">back to list</a>

    <a href="{{ path('app_education_form_edit', {'id': education_form.id}) }}">edit</a>

    {{ include('education_form/_delete_form.html.twig') }}
{% endblock %}
