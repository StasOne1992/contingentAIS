<form method="post" action="{{ path('app_system_serivces_delete', {'id': system_serivce.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ system_serivce.id) }}">
    <button class="btn">Delete</button>
</form>
