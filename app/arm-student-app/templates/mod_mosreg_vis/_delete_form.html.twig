<form method="post" action="{{ path('app_mod_mosreg_vis_delete', {'id': mod_mosreg_vi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mod_mosreg_vi.id) }}">
    <button class="btn">Delete</button>
</form>
